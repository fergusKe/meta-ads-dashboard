# 必填：OpenAI API Key（AI 分析與建議功能）
OPENAI_API_KEY=your_openai_api_key_here

# 可選：多組 OpenAI API Key，以逗號分隔供輪替
# OPENAI_API_KEYS=key1,key2,key3

# 選填：若要改用特定模型可取消註解並填入，例如 gpt-4o-mini
OPENAI_MODEL=gpt-5-nano

# 選填：若要啟用圖片生成功能請填入 Gemini API Key
GEMINI_API_KEY=your_gemini_api_key_here
GEMINI_IMAGE_MODEL=gemini-2.5-flash-image

# ============================================
# Agent 快取設定（效能優化）
# ============================================

# 是否啟用 Agent 結果快取（預設 false）
# 開發階段建議關閉，以便測試調整效果
# 正式環境建議開啟，可節省 40-60% API 成本
ENABLE_AGENT_CACHE=false

# Agent 快取過期時間（秒），預設 3600 (1小時)
# 只有在 ENABLE_AGENT_CACHE=true 時生效
AGENT_CACHE_TTL=3600

# 其他設定皆已提供預設值，需要調整時再參考 README 或程式碼註解

# 日誌與監控設定
AGENT_LOG_FILE=logs/ai_agents.log
AGENT_LOG_LEVEL=INFO

# 進階：成本與歷史紀錄檔案路徑
LLM_USAGE_FILE=data/history/llm_usage.csv
AGENT_HISTORY_DIR=data/history
